<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        button{
            margin-top: 10px;
        }
    </style>
</head>
<body>
    
    <% if(validacao){ %>
        <%console.log(validacao)%> 
        <% } %>

        <%console.log(sala)%>

        <% if(validacao){ %>
            <div class="row">
                <div class="col-md-12">
                    <div class="alert alert-danger">
                        <strong>Atenção!</strong>
                        <ul>
                            <!--Loop-->
                            <% for(let i = 0; i < validacao.length; i++){ %>
                            <li><%= validacao[i].msg %></li>
                            <% } %>
    
                        </ul>
                    </div>
                </div>
            </div>
            <% } %>

        <h1>Teste</h1>

        <form class="register-form" method="post" action="/cadastro">
            <h2>Cadastro</h2>
            <input type="text" placeholder="Nome:" name="name" value="<%= valores.nome_user%>" /> <br>
            <input type="password" placeholder="Senha:" name="senha" value="<%= valores.senha%>" /> <br>
            <button>Cadastrar</button><br>
        </form>
</body>
</html>